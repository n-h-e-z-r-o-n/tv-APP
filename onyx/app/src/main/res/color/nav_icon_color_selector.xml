<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Focused state -->
    <item android:color="?attr/SidebarFocus" android:state_focused="true"/>
    <!-- Pressed (optional) -->
    <item android:color="?attr/SidebarActive" android:state_selected="true"/>
    <!-- Default -->
    <item android:color="?attr/SidebarDefault"/>
</selector>


